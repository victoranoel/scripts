rule:
	@echo "in subdir make"
	@echo "make is " $(MAKE)
	@echo "This is the orginal makefile in master 0"
	@echo "This is the commit to the branch before PR"


all:
	I am adding this change to master after PR set up"
